import { test, expect } from "@playwright/test";
import url from "../config/strapiUrl";

// test("strapiStatus", async ({ request }) => {
//     const response = await request.get(url);
//     await expect(response).toBeOK();
//   });